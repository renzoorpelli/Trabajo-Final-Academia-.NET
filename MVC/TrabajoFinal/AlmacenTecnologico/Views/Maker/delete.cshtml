@model AlmacenTecnologico.Models.ViewModel.FabricanteViewModel

@if (Model is not null)
{
    <div class="align-content-center">

        <div class="mb-3">
            <h1 class="display-4">Eliminar  Fabricante:</h1>
            <hr />
        </div>
        <form method="post">
            @Html.HiddenFor(l => l.Id)
            <div>
                <div class="card text-center card text-bg-danger mb-3">
                    <div class="card-header">
                        Identificador Fabricante : @Model.Id
                    </div>
                    <div class="card-body">

                        <p class="card-text">Nombre: @Model.Nombre</p>
                        <input type="submit" class="btn btn-danger" value="Eliminar Fabricante" />
                        <a asp-area="" asp-controller="maker" asp-action="index" class="btn btn-primary">Volver al inicio</a>
                    </div>
                </div>
            </div>
        </form>

    </div>
}
else
{
    <p>No hay registros para eliminar :(</p>
    <a asp-area="" asp-controller="maker" asp-action="index" class="btn btn-primary">Volver al inicio</a>
}